require 'spec_helper'

describe "Sessions" do

  describe "GET /event/1/sessions" do
    it "shows only the sessions of the current event" do
      # @event = Factory.create(:event)
      # visit event_sessions_path(@event)
      pending
    end
  end

end
